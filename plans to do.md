## **Как усилить ещё (позже)**

- Реранкер (в ТЗ, не реализован)
- Фильтрация чанков по `user_type` в metadata Qdrant
- Порог «пустого RAG» → эскалация вместо выдуманного ответа

**Вывод:** агент + сценарии делают поиск **контекстно точнее**, особенно там, где сейчас «первый попавшийся ответ». Сам движок поиска остаётся тем же — меняется **момент** и **формулировка** запроса к нему.